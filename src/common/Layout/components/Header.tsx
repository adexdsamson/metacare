import React from 'react';
import { HeaderTypes } from '../types';


export const Header = (props: HeaderTypes): JSX.Element => {
  return <header className='w-full py-2 px-4 flex items-center justify-between'>

  </header>;
};